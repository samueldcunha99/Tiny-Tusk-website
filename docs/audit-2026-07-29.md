# Standing audit — 2026-07-29

## Every page in the home page's composition — 2026-09-25

User: "have u aplied the design to every page the parents corner blod
individual pages are still unchanged apply the redesign we did to all pages".
Each page was looked at in the browser before it was changed.

| # | Item | Status |
|---|---|---|
| 81 | **The seven Parents' Corner posts recomposed** (item 72). Before: a coral masthead holding a cobalt panel, rounded point boxes, coral numerals, "Read next" cards. Now: a masthead in the post's colour where that colour can carry words (canary; cobalt for coral and powder posts), with the question as the `h1`, the opening words, and the photograph plain, or the post's drawing on a disc of its `fill`. The sections follow, numbered like the guide's contents page (p2): on powder, with any section that carries a list on a canary chapter of its own. "Read next" shows the next three posts in the clinic's order as the home strip's speech bubbles (`QuestionBubbles` and `AllQuestionsLink`, exported from `ParentsCorner.tsx`; `POST_ART` moved to `content/parents.ts`). The page ends on the cobalt close with the post's own last line. | DONE |
| 82 | **Games arcade recomposed** (item 54). The three games are colour chapters (powder, canary, then cobalt running into the footer) instead of rounded, shadowed panels. The pieces are colour discs and the controls are underlined words. The game logic is unchanged. The page's eyebrow is a `SectionMarker`. | DONE |
| 83 | **Book a visit without boxes.** The step list and the form sit on the powder ground, and only the fields are white. The heading is display size under a `SectionMarker`. The steps are number discs: white ahead, cobalt for the current step, canary with a tick once done (a done step can be revisited). The list shows on desktop only: on a phone it pushed the form below the fold, and the rail above the form shows the same step. Fixed while here: each step's drawing wrapped onto a line of its own (now pinned to the question's first line); the fields inherited the label's letter-spacing and weight; the Continue arrow pointed up (a leftover rotation); the progress rail's track was powder on powder (now white) and sat centred in a wider column (now left-aligned); the consent note was cobalt-80 on powder, about 3:1 (now full cobalt). The "not connected" note and send errors are now plain text with the coral dashes instead of boxes. | FIXED |
| 84 | **Page endings.** `/dr-nupur`, `/services` and `/faq` ended on a powder button and then cut straight into the footer's ribbon. They now dip into the cobalt close (`/services` keeps its own words: "Not sure which one you need?"). `/book` and the 404 end on `SmileIntoFooter` (`SmileEdge.tsx`): the smile plus a short cobalt run, so the curve never lands on the ribbon's words. The 404's heading moved to the display scale. Every route now reaches the footer through cobalt. | FIXED |
| 85 | **Article photographs.** The article masthead forced 4:3, which cut the fluoride photo's built-in captions at both sides and the decay photo's leftmost arrow. It is now 16:10, as on the index. A centred crop also cut the thumb out of the thumb-sucking photo, on both the index and the article; `ArticleImage` now crops from the bottom, because every subject is a mouth, low in the frame. | FIXED |
| 86 | **The home hero is coral, by the user's decision** (user: "the light blue landing page is very low contrast", "the full screen i was saying", then chose "Orange, white words" over a blue text panel and a deep-blue screen, after being told the cost). Powder field → coral, with the canary loop kept (canary on coral is the p24 high-contrast pairing). The heading and paragraph are white: 2.98:1, under the 4.5:1 body text needs and the 3:1 for large text, so Lighthouse flags them (home accessibility 96 on mobile). The logo lockup and the "Meet Dr. Nupur" link stay cobalt: white vanished where the loop runs behind them (user: "now we are ot able to see this switch too dark blue"). The p34 dashes are canary so they show on coral, and the hero's smile into Dr. Nupur is coral. The loop's placement is unchanged; checked at 360×740, 375×667, 390×844, 430×932, 768×1024, 1024×768, 1280×720, 1440×900 and 1905×926, the white heading and paragraph never cross it, and it stays inside the section. This is the one exception to hard rule 1; CLAUDE.md §6.1 records it. | CHANGED (user decision) |
| 87 | **The intro lost its name** (user, sending the guide's cover: "i want this as my intr screen"). Since item 78 the cover arrives in its opening pose from CSS, with the name lowered by `translateY(110%)`. GSAP read that CSS lowering as a pixel `y` and animated only `yPercent`, so "Tiny Tusk" and "Pediatric Dental Clinic" finished their rise still out of sight, below their masks: only the roundel ever showed. The rise now pins `y: 0` at both ends (`Preloader.tsx`). Checked by freezing the timeline at 1.7 s at 390 and 1905 wide: the full cover shows, white roundel with canary name and tag, and it then lifts away into the hero as before. The full cover holds for about half a second before the lift (at 1.9 s). | FIXED |
| 88 | **The hero loop sat behind the button and the scroll link on a phone** (user, with a screenshot: "ribbon position is not good"). Canary on the canary button blurred its shape, and the tail ran through "Meet Dr. Nupur". Below `lg` the loop now has a band of its own (`Hero.tsx`): a third of the spare height stays above the words, as `my-auto` gave, and the band takes the other two thirds plus the link's row. The loop scales to fit the band, right of the link, so no phone or tablet height can push it into the words, the button or the link. From `md`, where the button no longer spans the screen, the band reaches up beside it. It had also run behind the heading and paragraph on landscape phones and small windows (1000×768 in the `md` range). On the narrowest desktops (`lg`, below `xl`) the loop sits further right: at 1024 its tail touched the name's dashes and ended 13px from the paragraph. Checked at 360×640, 375×667, 360×740, 414×736, 390×844, 430×932, 844×390, 768×1024, 820×1180, 1000×768, 1024×768, 1152×864, 1280×720, 1440×900 and 1905×926: the loop touches no word, the button or the link; on desktop it is more than 60px from any word; it stays inside the section with no sideways overflow, and it still draws in after the intro. On 736–740px-tall phones there is little spare room, so it is small there (about 130–140px wide). | FIXED |

Validation: typecheck and build pass (18 pages, 404, sitemap, llms.txt).
Checked on every page changed in this pass: all 13 at 390 (`/book`,
`/dr-nupur`, `/services`, `/faq`, `/games`, the 404 and all seven posts), and
the six pages plus one post at 1440. Each had one `h1`, no horizontal overflow,
0 artworks crossing a section's top or bottom edge, 0 reveals left pending and
0 strokes left undrawn. The production build hydrates with a clean console on
all 18 paths checked, including `404.html`. (`vite preview` answers an unknown
path with the home page's HTML, so the 404 was checked at `/404.html`, the
file Netlify serves.) Lighthouse on mobile for `/book`, `/games` and a post:
100 / 100 / 100 / 100. With reduced motion forced on the component layer
(`/book`, `/games` and a post), no strokes were hidden, nothing was split and
nothing had moved. The booking form was walked through all four steps on a
phone.

## SEO, accessibility and speed — 2026-09-25

The user sent the live site's reports (an SEO checker, and PageSpeed Insights
desktop and mobile: Performance 87 / 82, Accessibility 92, SEO 92, Agentic
Browsing 1/4) and asked for all of it fixed. Every finding was reproduced
locally first (Lighthouse on a production build: 92 / 100 / 92 / 33).

| # | Item | Status |
|---|---|---|
| 73 | **Every page was an empty shell.** Crawlers that run no JavaScript saw 0 words, no H1, no headings and almost no links (the SEO checker's five errors). Every route is now prerendered to its full HTML (`entry-server.tsx` renders `<App>` with the streaming renderer, which waits for the lazy site chunk; `scripts/prerender.mjs` writes it into each route's file) and the browser hydrates it. Home: 1,076 words, one H1, 27 headings, 55 internal links. Audit item 52's blocker ("once the full site prerenders") is gone. | FIXED |
| 74 | **The Netlify catch-all (`/* /index.html 200`) answered every missing file with the home page.** That was robots.txt's "89 errors", llms.txt's missing H1 and links, and ai-catalog.json's invalid schema. Now: a real `public/robots.txt`; `sitemap.xml` and `llms.txt` generated from the route table; no catch-all, and a prerendered `404.html` that Netlify serves with a real 404 (so ai-catalog.json is correctly "not applicable"). | FIXED |
| 75 | **Netlify 301s `/page` to `/page/`**, so every internal link cost a redirect, and canonical links would have pointed at redirects. All 43 internal links, the canonicals (new, one per page), `og:url`, the sitemap and llms.txt use the slashed address. `og:image` is absolute now that the domain is known. | FIXED |
| 76 | **Accessibility.** SplitText put `aria-label` on paragraphs waiting to reveal (prohibited on `<p>`; also the agent "accessibility tree" failure): now `aria: 'none'`. Footer tagline strip canary on cobalt-80 3.9:1 -> white 4.88:1. Map placeholder 3.08:1 -> white. Nav logo link named by its own text. Booking: field labels and the label row were faded cobalt (3.8:1), the progress rail's wrapper carried a stray `aria-label`. Article section numerals were coral type (1.84-2.78:1) -> cobalt. | FIXED |
| 77 | **Speed.** The stylesheet is inlined (it was the one render-blocking request); the lazy site chunk and all three fonts are preloaded; a useless preconnect to the site's own origin is gone. The clinic photographs have 640/1024px WebP copies with `srcset` (`tools/responsive-images.py`; 310 KiB -> 135 KiB at desktop). Scroll entrances use two shared IntersectionObservers (`onArrival` in `motion.ts`) instead of a ScrollTrigger per element, and nothing is split or hidden until it nears the screen: forced reflow at load fell from 744 ms to 27 ms (mobile trace, 4x CPU). Netlify `_headers` caches built scripts and fonts for a year. | FIXED |
| 78 | **Nothing on screen is hidden to be animated again.** Prerendered content is painted before any script runs, so re-hiding it would blink. Content on screen when the page wakes stays as it is; only what scrolls in draws or rises. The hero's entrance plays only behind the first-visit intro, and the intro cover itself is in the HTML, shown from the first paint by a tiny head script (`html.tt-intro`). | CHANGED |
| 79 | **Server response time (0.56 s)** is Netlify's edge, not the code: the pages are static files. The HTML is larger now (home: 216 KiB gzip, about 70-86 KiB with the Brotli Netlify serves). | NOTE |
| 80 | **Speed Index** is set mostly by the first-visit intro (about 2.7 s, a client decision), which now at least paints from the first frame instead of after the scripts load. | NOTE |

Validation: typecheck and build pass, 18 pages + 404 + sitemap + llms.txt
written, no mock strings in `dist/`. Lighthouse 13.4.1 on the production
build: home 100 / 100 / 100 / 100 on desktop and mobile; `/book`, `/dr-nupur`,
`/services`, `/laughing-gas`, `/journey`, `/inside-clinic`, `/games`,
`/parents-corner`, `/faq` and an article all 100 / 100 / 100 / 100 (mobile); the
404 page's SEO 63 is its intentional `noindex`. Hydration checked with a
development React build on all 19 paths, first and repeat visit: no warnings.
Repeat visits: no painted text or stroke is hidden after hydration on any page.
Scrolling from the top, 19 of 19 reveals and 59 drawings still animate in.
Mobile trace (Slow 4G, 4x CPU, local gzip): LCP 1.71 s, CLS 0, no
render-blocking requests.

Not verified: the live Netlify deploy (the 404 status, headers and Brotli
sizes are from Netlify's documented behaviour and the current live site), and
PageSpeed's own run of Lighthouse 13.5.

## Standalone pages — 2026-09-24

User: "Parents corner, inside clinic, laughing gas — redesign these page like u
did for the full home page too", then "see and do nothing blindly". All three
were screenshotted at 390 and 1440 before any edit.

| # | Item | Status |
|---|---|---|
| 70 | **Parents' Corner, Inside the Clinic and Laughing Gas recomposed like the home page.** Before: Parents' Corner was seven rounded tinted cards on paper; Laughing Gas was the old card layout (a crude placeholder tile, rounded panels, white step cards, bordered FAQ rows, a coral box whose two-line CTA broke its ellipse); Inside the Clinic was one flat powder section ending on a hard edge. Both of the first two also carried coral type (`SectionNumber tone="coral"`, about 2:1 on paper), and `CoralPageAccent`. Now each page is colour chapters joined by `<SmileEdge>`, no boxes, reveal and draw only, and each ends on the cobalt close running into the footer. **Parents' Corner:** powder (the home strip's header, with a canary toothbrush scaled up behind it, p32) → canary → cobalt → powder, two posts to a chapter (side by side from `md`, the second a step lower), the last set as a spread; the client's order kept (photograph, question, summary), photographs plain; the two posts with no photograph show their drawing on a disc of their `fill` instead of a "photograph on its way" tile. **Inside the Clinic:** powder (heading, disclosure, the three concept visuals as a gallery: stacked, two over one on a tablet, 7 + 5 over 12 on desktop) → canary (the vision complete and verbatim, beside the six amenities on colour discs, the treatment discs' cycle) → cobalt close. The home section is unchanged. **Laughing Gas:** powder welcome with a badge (the treatment's drawing on a cobalt disc ringed with its names, p35) → cobalt "straight answer" (official register, both lists side by side) → canary four steps on discs → powder questions (the home FAQ's rows, now shared as `QuestionRows` in `Faq.tsx`) → cobalt close with the page's own words. Copy unchanged apart from layout. | DONE |
| 71 | **The home page's booking close is now `sections/BookingClose.tsx`**, shared with the three pages. Its canary loop showed from `md` and ran through "Ready for their" at 768–1023px (on the home page too); it now shows from `lg`, where it clears every line (checked against the text boxes at 1024 on the longest heading). | FIXED |
| 72 | **The seven article pages (`/parents-corner/<id>`) still use the older banded layout**: a coral masthead holding a cobalt panel, rounded point boxes, coral numerals, "Read next" cards. Not in this pass; the user named the three nav pages. | DONE (item 81) |

Validation: typecheck and build pass, 18 route heads, no mock strings in
`dist/` (the only "TODO" there is the existing og:image comment in
`index.html`'s head). All three pages at 360, 390, 768, 1024 and 1440: one
`h1`, no horizontal overflow, 0 artworks crossing a section's top or bottom
edge, 0 reveals left pending, 0 strokes left undrawn, clean console. With
reduced motion forced on the component layer: 0 of 175 `[data-draw]` strokes
hidden, nothing split, no text transformed. The home page is unchanged at 390
(11,338px, 0 of 72 artworks crossing an edge).

## Mobile-first redesign — 2026-09-23

Brief from the user: audit the site, then redesign it with the phone as the
primary target. The client wants it "very beautiful", with transitions and
animation, every element drawn from the brand book, "a proper flow and not many
boxes". The uncommitted 2026-09-15 / 09-22 working tree was audited first and
preserved before any edit as tag `checkpoint/pre-mobile-redesign-2026-09-23`
(a snapshot commit off `e7d72cc`, untracked files included; the branch itself
was not moved). `git diff checkpoint/pre-mobile-redesign-2026-09-23` shows
exactly what this pass changed.

### What the audit found (items 40–69)

| # | Item | Status |
|---|---|---|
| 40 | **Invented clinical copy.** The `Services` "pillars" gave all sixteen treatments descriptions nobody supplied — "mercury-free", "hospital-grade sleep care", mouthguards "engineered to protect", children who "test our gentle tools on their fingernail first" — although `treatments.ts` rules description copy out in so many words (CLAUDE.md §1). Removed. Services now show the client's own labels and drawings and nothing else. | FIXED |
| 41 | **Agent-written FAQ answers giving clinical advice.** Four of the eight answers were written by a build pass (commit `9b98592`), not the clinic. The toothpaste answer says "a pea-sized smear for toddlers", where standard pediatric guidance is a rice-grain smear until three; others recommend six-monthly recalls and claim the clinic is "designed around neurodiversity". Moved to `FAQS_AWAITING_REVIEW` in `content/faq.ts`, not rendered, grammar fixed ("an age-appropriate"). | OPEN (needs Dr. Nupur's sign-off) |
| 42 | **Other invented or misleading copy removed.** Hero badges "Ages 0 to 14" and "Now Booking"; a journey hinge calling the elephant "Ria" (Ria is the girl in the p34 photograph); designer "stroke concept" captions and a visible "Single-Stroke Construction · Brand Identity Guide p. 03" line; a home-page "What happens next" process ("our clinic team calls to confirm details", "we share gentle prep tips"); portrait tiles captioned "Tiny Tusk Photography" although no photography exists. The closing chapter now uses the booking page's own line ("a kind person will call to make the rest simple"). | FIXED |
| 43 | **Client decisions silently reverted by the uncommitted tree, now restored.** The 44px phone nav mark (the client asked three times for a thinner bar; the exception is documented in `Logo.tsx`); no roundel in the phone hero (commit `be2791d`); the "Welcome to / Tiny Tusk" headline (the guide's p34); the "How a visit goes" label (renamed "The Logo Story"); the p3 `LogoStory` row, which had been orphaned (the client asked for it "as it is in the pdf, same page, a small one"); the floating WhatsApp disc (the number was supplied for it); and `content/journey.ts` restored verbatim, since it is the voice reference. | FIXED |
| 44 | **The phone home was 16.9 screens, and its ground flipped between powder and canary eight times.** That undid item 37's one-pass colour run. It now makes one pass — powder → paper → canary → coral → cobalt — and measures 12.5 screens at 390×844, 10.4 of them content and 2.1 the footer. | FIXED |
| 45 | **Contrast failures.** Dr. Nupur's credentials were coral type on powder/canary (1.84–2.39:1). The timer's "2:00", "Ready" and round labels were cobalt type on coral (2.67:1, hard rule 1). Credentials are now cobalt; the timer's words sit on a cobalt disc in white and canary. | FIXED |
| 46 | **High-contrast loops crossing type.** A white loop ran through the white "Ready for their first visit?" copy on cobalt and erased the words it crossed, and canary/white loops sat behind body copy elsewhere. A white loop also covered two service labels: a stacking bug, where a non-positioned strip painted under an absolutely positioned loop. Every loop has been repositioned clear of reading copy; the booking loop is desktop-only; the strip is positioned. | FIXED |
| 47 | **`BrandImage` drew a grey rectangle behind every ellipse title** (`shadow-lg` on the link box), and its placeholder said "Tiny Tusk Photography". Both removed. | FIXED |
| 48 | **Accessibility.** `MixedWeightLabel` put the space between its two words in an `aria-hidden` span, so every CTA's accessible name ran together ("ScheduleAppointment"); it is now a real no-break space. The timer announced every second through `aria-live`; it now announces rounds, pauses and the finish, and the clock is `role="timer"`. | FIXED |
| 49 | **Hard rule 3 and motion-rule breaches** in the tree: raw `rgba()` in the journey's auras and wave, blur halos, `animate-ping`, gradient strokes, fade-ins. All removed with the rewrite. | FIXED |
| 50 | **Dead code deleted:** `Team.mobile`, `Services.mobile`, `InsideClinic.mobile`, `BookBand.mobile`, `PhoneButton`, `content/services.ts`, and `home.css` (every rule in it was a no-op or a duplicate). | FIXED |
| 51 | **Sub-pixel seams** between same-colour sections at 2x density (sections land on fractional pixels). `.tt-section` now overlaps the next section by 1px. | FIXED |
| 52 | **Route transitions.** Native cross-document View Transitions were built and removed. On a client-rendered site the incoming frame is blank, so the page "arrives" empty and fills in afterwards, and Chrome logs an uncaught promise rejection on every navigation. Revisit once the full site prerenders (item 39's #419 note). | OPEN (blocked) |
| 53 | **`lenis` is a dependency that nothing imports** (and `index.css` still carries an `html.lenis` rule). Remove it, or adopt it deliberately. | OPEN |
| 54 | **`GamesArcade` is the last card-heavy surface**: three games on rounded, shadowed panels. Game boards arguably need a frame; it is a design decision rather than a defect. | DONE (item 82) |
| 55 | **`docs/brand-coverage.md`** predates this composition and needs re-mapping. | OPEN |
| 56 | **Loops cut by section edges** (user review, same day: "i dont want cuts like this"). The booking loop hung past its section and was sliced flat where the cobalt footer began; the hero, Dr. Nupur and services loops were clipped the same way at their bottom or top. Every loop and scaled-up illustration now sits wholly inside its section top to bottom, and runs only off the left and right, as the guide crops at the page edge. An automated edge check (each artwork's stroke box against its section box) finds 0 crossings on all 11 routes at 390, 768 and 1440px, and on the home page at 360–1920px. The 404's `LoopField` was replaced by a contained loop for the same reason. | FIXED |
| 57 | **Colour balance** (user review: "add a bit more colour", then "it suddenly has too much yellow"). Three canary chapters in a row followed nearly four screens of pale paper. The home page now has no paper: powder is the ground, and the other colours are chapter plates set into it, the way the guide slots its colour chapter pages between reading pages. Cobalt for how a visit goes (the guide's own "The Logo" page is cobalt), canary for the treatments only (about one screen, down from 2.7), coral for the game, cobalt for booking and the footer. The FAQ's toothpaste is canary on powder (p32 exactly); the booking loop is canary on cobalt (p24). This supersedes the single powder→paper→canary→coral→cobalt run described in item 44. | FIXED (awaiting client view) |
| — | **The hero CTA's outline never drew until the page was scrolled.** Its scroll trigger was measured while the entrance held the button 110% down its mask. The hero now calls `ScrollTrigger.refresh()` when its entrance lands. | FIXED |
| 58 | **Parents' Corner joins the home page**, first as the guide's p32 post tiles (canary and cobalt tiles carrying Dr. Nupur's own questions set large, with one brand illustration scaled up behind each; the tiles were removed in item 63). Illustrations rather than photographs on purpose: two of the article photographs are clinical close-ups of decay and one has stock captions baked in, and the client asked for the photographs to stay plain, so nothing is laid over them. The first four questions show; all seven are one tap away. | ADDED |
| 59 | **Tap-to-replay drawings.** `<Doodle tap>` redraws a drawing by hand when a child taps it: the journey glyphs, the heart and face rings, the clinic amenities, the review heart and the timer's doodle. Decorative only, and skipped under reduced motion. | ADDED |
| 60 | **The journey is a storyboard.** The p3 row lights the stage being read (`<LogoStory active>`). On desktop that is the row in the sticky column. On a phone a slim rail of the same five glyphs sticks under the nav while the beats are on screen; tapping a stage scrolls to it. | ADDED |
| 61 | **Every route has its own HTML file and head.** `content/routes.ts` is now the single source of route titles and descriptions: `Site.tsx` reads it, and `scripts/prerender.mjs` writes `dist/<route>/index.html` for all 18 routes with their own title, description, Open Graph and Twitter tags (and `noindex` where needed). Links shared on WhatsApp, and crawlers that run no JavaScript, now see the right page. `og:image` pointed at a file that did not exist; `public/og-image.png` is now the brand cover (p1) at 1200×630. Full-body server rendering remains open (item 52). | FIXED (body SSR still open) |
| 63 | **Boxes and empty space** (user review: "dont like these boxes also lots of empty spaces", pointing at the Parents' Corner tiles and the empty cobalt band between booking and the footer). Parents' Corner is now open questions on the powder ground, each under one hand-drawn coral illustration that draws in and replays on tap: a swipe row on a phone, four across on desktop, with the desktop heading balanced over two lines. Section padding came down across the home page (about 320px in total on a phone; the home page now measures 10,920px at 390 wide), and booking now runs into the footer as one cobalt surface: the visual gap from its last line to the roundel roughly halved, from about 160px to about 80px on a phone. On desktop the home sections started on three different lines (40, 52 and 92px at 1440): clinic, services and Parents' Corner padded inside a 1320px box, the rest outside it. They now share one 1320px column; the hero stays aligned with the nav. The FAQ's canary toothpaste moved up and shrank slightly on desktop, so it still sits behind the headline (p32) but clears the paragraph. | FIXED |
| 64 | **Treatments on colour discs** (user: "can we find some other colourful way to show treatments?"). Each drawing now sits on a cobalt, powder or coral disc. The three-step cycle never puts equal discs side by side in the two-row strip, and every drawing takes its disc's p24 partner (canary on cobalt and coral, cobalt on powder). Accents that would vanish into their disc are re-tinted in CSS: coral turns canary on coral, powder turns white on powder. The line is 2px on a disc, because canary on coral is the weakest legal pairing and a hairline of it all but disappeared. The home discs wipe in clockwise, one after another (the timer's ring, drawn), and drop their dash once they land, since a dashed stroke left a hairline seam at 12 o'clock. `/services` uses the same discs. | ADDED |
| 65 | **The footer's tagline marquee is back** (user: "i liked the continouse strip of moving text in the footer"). The 2026-09-23 redesign had dropped it. The committed version jumped once a lap: three copies slid by half the track. It now uses two identical halves, so the loop never jumps, and runs at the old unhurried pace (90s a lap, about 25px a second). It stands still under reduced motion (existing CSS rule). | RESTORED |
| 66 | **Parent Voices and Parents' Corner redesigned** (user: "redesign this and this"). Voices is now a cobalt plate, set like the guide's p35 cobalt square: the headline across the full width, a large canary type-on-a-path stamp beside the review, and the parent's words in canary display type. That breaks what was three pale chapters in a row. Parents' Corner is a wave of speech bubbles: each question on the p33 stylised title ellipse, canary and cobalt in turn, with a coral drawing on its shoulder. `EllipseTitle` moved out of `BrushTimer.tsx` into `components/` so both can use it, and its outline no longer thickens when stretched (`non-scaling-stroke`). | REDESIGNED |
| 67 | **Welcome and Dr. Nupur ran together as "one long page box"; her photo needed a place.** Dr. Nupur is now a canary plate, with the welcome's powder dipping into it in the smile curve. The two canary chapters (Dr. Nupur, services) have two chapters between them. Her portrait is a round badge ringed with her name and specialty, the guide's p35 type on a path. Until an approved photograph exists it holds the heart on a cobalt disc; `DR_NUPUR.portrait.src` in `content/team.ts` takes the photograph (a code-comment TODO marks it). On desktop the headline runs across and the badge stands beside her words. On `/dr-nupur` it stays in view while the long text scrolls. | FIXED (photograph awaited) |
| 68 | **The starting screen rebuilt as the guide's cover** (user: "please fix the starting screen"). The intro typed the tagline flat in the body face over a separate arc drawn like an eyebrow, which imitated the roundel instead of using it, then flew the mark to a measured nav position. It is now page 1 of the guide: the white tagline roundel, the canary wordmark and "Pediatric Dental Clinic" on cobalt. The mark shows first; the ring sweeps in clockwise from 9 o'clock, so the tagline writes itself in reading order and the smile follows underneath (a two-layer CSS mask driven by one variable, geometry measured from the roundel). The name rises by clip reveal, and the cover lifts away with the tagline's smile as its trailing edge, then hands over to the hero. About 2.3s in all, once per session, skippable, and skipped entirely under reduced motion. This supersedes item 11. | FIXED |
| 69 | **The roundel was the wrong logo** (user: "use this instead of the 2nd image wrong logo"). `roundelPaths.ts` came from the client's indoor lollipop board, where the mark fills 60% of the smile's width; the guide's own tagline unit has it at 42%. `tools/extract-roundel.py` now extracts from the brand book's cover (page 1, in the repo), baking the live Avenir Next Condensed tagline to outlines, and fits it into the same 600-unit box, so every roundel on the site changed with no call-site edits: hero, footer, 404 and intro. The hero's desktop logo is now the full cover lockup (roundel, wordmark, "Pediatric Dental Clinic") in cobalt, the element colour for powder, top-aligned with the headline (user: "want the same logo here", "move it up"). Phones still show no roundel in the hero, as the client asked. The fonts were checked while here and are unchanged: Montserrat and Encode Sans Condensed are the free alternatives the client chose (message of 2 Aug 2026). | FIXED |
| 62 | **Two motion bugs found while building 58–60.** (a) Line masks padded top and bottom collapsed their negative margins, so a split paragraph was taller than the same paragraph unsplit; when a reveal reverted, everything below moved up about 36px and every scroll trigger drifted. Masks now pad the bottom only (split and unsplit heights match exactly). (b) A trigger written `"top top+140"` is not GSAP syntax (offsets use `+=`) and silently read as `"top top"`; the rail now uses `"top 140px"`. | FIXED |

Earlier items updated by this pass: **18** (reveal thresholds) is FIXED — the
whole site now reveals on one line, `REVEAL_START`. **27** (the through-line) is
PARTIAL — the journey's scroll-drawn thread is that stroke made literal, but a
site-wide stroke and route transitions (52) remain. **13** is fully closed (the
placeholder's false caption was the last piece).

### What the redesign is

- **A powder ground with colour plates** (item 57, in `Home.tsx`): welcome and
  Dr. Nupur on powder; how a visit goes on cobalt; inside the clinic on powder;
  the treatments on canary; parent voices and questions on powder; the brushing
  game on coral; booking and the footer on cobalt. Sections of one colour run
  together with no rule between them. Each change of colour is `<SmileEdge>`:
  the outgoing ground dips into the next in the curve of the tagline unit's smile
  (p14). No artwork crosses a section's top or bottom edge (item 56).
- **No boxes.** Content sits on the ground. The only framed things are the
  concept photographs, in the guide's p34 image-tile treatment, and the map.
  Coral carries no text anywhere: the game's title is set on a canary ellipse
  (p33–34) and its clock inside a cobalt disc, the disc the roundel is printed on.
- **The book's pages, one per chapter.** p16 (a powder field with a giant canary
  loop) for the welcome, set with p34's copy and dashes. p4's cobalt chapter page
  for the visit, carrying p3's glyph row in canary and white. p35's type on a
  path ringing the heart and the smiling face. p32's move of scaling an
  illustration up into a field (the canary toothpaste behind the FAQ heading, the
  toothbrush at 33% on coral). A cobalt page with a canary loop for booking.
  p36's roundel to close. Every asset on `ART` has a use on the home page.
- **Motion is two systems, never more:** *draw* (the hero loop, doodles, glyphs,
  CTA outlines and the journey thread) and *reveal* (text rising line by line out
  of a clip mask, via SplitText, which ships free with GSAP 3.13+). The journey's
  canary thread is scrubbed to the scroll, and each glyph draws as the thread
  reaches it. The hero's entrance waits for the preloader (`onIntroDone`), and
  `EASE.celebrate` still belongs to the timer's finish alone.
- **Mobile first, one component per section.** Every section is a single
  responsive component; the desktop layout is the same content arranged as the
  book's spreads (text left, art right; a sticky heading beside the journey).

### Validation (this pass)

`npm run typecheck` and `npm run build` pass; `git diff --check` is clean apart
from line-ending notices. `dist/` contains none of the mock contact strings, and
none of the removed invented claims.

- **Home, checked in a browser at 360×640, 390×844, 768×1024 and 1440×900:** one
  `h1`, `scrollWidth <= clientWidth`, and screenshots reviewed chapter by
  chapter. At 360×640 the hero fits one screen using its short-viewport copy.
- **Every route** (`/services`, `/journey`, `/dr-nupur`, `/inside-clinic`,
  `/games`, `/parents-corner`, `/faq`, `/book`, `/laughing-gas`, a 404): one
  `h1`, no overflow, no rendered `TODO`.
- **Production preview:** preloader → hero handover works; 18 of 18 reveals
  complete after a scroll; 47 of 47 drawn outlines complete. The console is clean
  on load and on link navigation. Timer start, pause, start over and live
  announcements work; the mobile menu opens beneath the 44px bar. The
  accessibility tree reads correctly (heading order, names, regions).
- **Reduced motion, both layers.** Component layer, with `matchMedia` forced to
  "reduce": the preloader is skipped, nothing is split, 0 of 169 `[data-draw]`
  paths are left hidden, and no text is transformed. CSS layer, in headless Edge
  with `--force-prefers-reduced-motion`: the hero renders complete.

- **Item 63 pass (dev server):** typecheck and build pass, 18 route heads
  written, no mock strings in `dist/`. The edge check finds 0 artwork crossings
  at 360, 430, 768, 1024, 1280 and 1440px. There is no overflow at any width,
  0 reveals are left pending after a scroll, and the console shows no errors or
  warnings. Under forced reduced motion the new strip renders unsplit, with all
  74 of its strokes complete.

- **Items 64–67 pass (dev server):** typecheck and build pass, 18 route heads,
  no mock strings in `dist/`. Home checked at 360, 390, 768, 1024 and 1440px:
  0 artwork crossings (43 artworks checked), no overflow, one `h1`, and 0
  reveals left pending. `/dr-nupur`, `/services` and `/games` were
  screenshotted. The console shows no errors or warnings. With reduced motion
  forced on the component layer, all 243 `[data-draw]` strokes are complete
  without scrolling and nothing is split.

- **Items 68–69 pass (dev server):** typecheck and build pass (the dev server
  now survives a build), 18 route heads, no mock strings. The intro was sampled
  every 100ms: the sweep completes in about 1s, the cover lifts, the "seen" flag
  is set and the hero plays its full entrance. The sweep was checked at 0, 90,
  130, 250 and 330 degrees. The new roundel was checked in the hero, the footer
  and the 404. The home page has 0 artwork crossings, no overflow and a clean
  console.

Not verified: real iOS Safari and Android hardware, a timer run all the way to
its finish, a Lighthouse run, and client approval of the new composition.

**Testing trap:** a browser tab or window that is backgrounded or occluded
throttles `requestAnimationFrame`, so GSAP tweens freeze partway and text looks
clipped. Bring the tab to the front before judging reveals. A full-page
screenshot also resizes the viewport, which fires any reveal that has not yet
played, mid-capture.

## Brand flow revision — 2026-09-15 (superseded by 2026-09-23)

Checkpoint: `checkpoint/pre-design-flow-2026-09-15` at `e7d72cc`.
Work branch: `codex/brand-flow-redesign`. The existing untracked local agent
settings were left untouched and were not included in the checkpoint.

**39 — Homepage composition revised.** `sections/Home.tsx` now owns one
responsive sequence: welcome, journey, services, doctor, clinic, brushing game,
parent voices, FAQ and booking. The separate mobile homepage and legacy
hero/journey variants were retired. After client review, the full palette was
restored to substantial fields: powder and coral in the hero, canary for
services and brushing, paper for reading, powder for parent voices, and cobalt
with coral artwork for the closing action. Coral carries graphics only.

Open service icons, a biography without a placeholder portrait, ruled amenity
lists, an open quotation and a staggered gallery replace repeated rounded
cards. The guide's mark, outlined wordmark, tagline roundel, five journey
glyphs, four doodles, lasso, dashes, arrow, zigzag, high/low loop fields,
three ellipse CTA fills, and arc/ring type all have homepage uses. Original
brand artwork is unchanged. Clinic concept labels and disclosures remain;
the current sourced testimonial and client-supplied biographies/clinic copy
are preserved. This is not a completed site-wide animated route-transition
system (item 27 remains open).

Journey advances only through user interaction and starts at Detection.
The mobile navigation retains the 64px mark and provides a direct phone link.
Floating phone/WhatsApp controls no longer cover content; WhatsApp remains in
navigation. Footer links now resolve to real routes, including from subpages.

Production QA caught an existing React #419 hydration error: `renderToString`
was emitting an unfinished lazy Suspense boundary. The holding screen still
prerenders; the full browser-routed site now explicitly starts with an empty
root and client rendering. Full-site SSR remains future work.

Validation: TypeScript and production build pass. Production homepage checked
at 360, 390, 768, 1024 and 1440px: `scrollWidth <= clientWidth` and one `h1`.
Desktop/mobile screenshots inspected; journey selection, FAQ expansion,
timer start/pause/reset, and mobile menu checked. Production startup console
is clean after the hydration fix. Mock contact strings absent from `dist`.
Reduced motion was **simulated in a local QA harness**, since the browser
connection lacks native media emulation: the component preference branch and
three actual CSS media-rule groups were activated, no animated content was
hidden, and every `[data-draw]` computed offset was zero. An OS-level
reduced-motion test remains distinct from that simulation. Backend deployment
and clinic content approvals are outside this pass.

The older dated entries below are historical; this revision supersedes their
homepage layout and palette descriptions.

The site was audited section by section in a real browser (desktop 1440×900,
mobile 390×844). This is the **standing to-do list**. Item numbers are stable —
quote them in commits and hand-offs.

Status: `FIXED` (Phase 1a) · `OPEN` · `CONFIRMED OK`.

---

## Integrity — content that must not ship as-is

| # | Item | Status |
|---|---|---|
| 1 | **Testimonials were fabricated.** Four invented parents and children ("Amelia, parent of Ivy" etc.) were presented as real reviews. Removed. `content/testimonials.ts` now holds clearly-marked placeholders behind an `AWAITING_REAL_TESTIMONIALS` flag; the section renders an honest "awaiting content" state. **Real quotes + written consent + agreed attribution must come from the clinic before the flag is flipped.** | FIXED (blocked on clinic) |
| 2 | **Raw `TODO:` strings rendered in the UI** under the Dr. Nupur portrait, and again in the "warm human detail" panel. Both removed; notes now live in code comments, and the panel renders only when `hasFavouritePart` is true. | FIXED |
| 3 | **Photography licensing unconfirmed.** Everything in `public/images/` was extracted from the client's brand book. Licensing for web use is unverified. Noted in `README.md`. Must be cleared with the client before launch. | OPEN (clinic) |
| — | **Footer and nav contact details were unverified placeholders.** ~~Address~~, hours, ~~phone~~ and email were dev-only behind a "not clinic information" label. **The address is now client-verified** (supplied 2026-08-01) and ships in the footer, the JSON-LD `PostalAddress`, and a real keyless Google Maps embed with a "Get directions" link. **The phone is now client-verified too** (`+91 96190 00971`, supplied 2026-08-14) and ships as `CLINIC_PHONE` in the footer, the holding screen and the JSON-LD `telephone`. Email and hours remain unverified and stay behind the dev flag, whose label now reads "email and hours only". | ADDRESS + PHONE FIXED (email/hours blocked on clinic) |
| — | ~~**WhatsApp number is not yet supplied.**~~ `WHATSAPP_NUMBER` is now the client-verified `919619000971` — the same line as `CLINIC_PHONE`. The button is live in production; the UK dev sample is gone from `dist/index.html` (checked) and the coral "unverified" marker no longer renders. The holding screen carries the button too, since the pre-opening gate hides every other route. | FIXED |
| — | **Dr. Nupur's credentials are CLIENT-VERIFIED.** `BDS · MDS, Pediatric Dentistry` is genuine and confirmed by the client. An earlier audit wrongly flagged it as invented. Do not remove; see the protective comment in `content/team.ts`. | CONFIRMED OK |

## Mobile

| # | Item | Status |
|---|---|---|
| — | **The mobile home was a 7.5-screen scroll.** Measured at 390×844: Journey alone was 2.57 screens, with Services and the full Booking form adding another 2.08. The home page now renders only `Hero` (00), `GuidedEntryMobile` (01) and the new `ExploreMore.mobile.tsx` — a 2-up tile index plus a booking CTA band. Production measures **4.29 screens, of which 2.93 is content** and the rest is the footer. Journey gained a real `/journey` route (it had none) and an `asPage` prop like every other routed section; `guidedEntry.ts` now points there instead of the dead `#journey` anchor. Nav and footer link lists both carry it. Footer trimmed on mobile only, behind `md:`. | FIXED |

## Bugs

| # | Item | Status |
|---|---|---|
| 4 | `fetchPriority` → `fetchpriority` in `BrandImage.tsx` (React 18 logged a console error). | FIXED |
| 5 | Two `<h1>` on the home page. Hero keeps the `h1`; Team demoted to `h2`. | FIXED |
| 6 | Dr. Nupur's philosophy quote rendered twice (blockquote + arc). The arc now carries the brand tagline instead. | FIXED |
| 7 | `<Circled>` mis-sized — the lasso cut through the words it should enclose. Root cause: the SVG letterboxed without `preserveAspectRatio="none"`. Added that prop through `Doodle` → `BrandArtView`. | FIXED |
| 8 | Parents' Corner card titles clipped mid-word and the third card overflowed. Root cause: the whole multi-word title was wrapped in `<Circled>`, whose `nowrap` prevented line breaks. The lasso now sits only on the section heading, where the guide intends it. | FIXED |
| 9 | Section numbering skipped `05` (a slot was reserved for a Brush section that did not exist). It now runs continuously 00–11 after Brush Timer, Inside the Clinic, and Start Here were added. **Every section looks itself up by id, never by array index** — via `useSectionMeta(id)` since item 29. | FIXED |
| 10 | `/does-not-exist` silently rendered the homepage. The native pathname switch now sends every unknown URL to `<NotFound>`. | FIXED |

## Open — visual tuning (Phase 1)

| # | Item |
|---|---|
| 11 | **Preloader's final move uses hardcoded percentages** (`xPercent: -165, yPercent: -125`) to land on the nav slot. It will not land accurately across viewports; it should measure the nav logo's real position. **Superseded (2026-09-23): the intro no longer flies to the nav at all; see item 68.** |
| 12 | **Ria's story indicator now reflects the selected moment.** The three accessible story tabs and Previous/Next controls drive a real progress bar and featured story panel. **FIXED** |
| 13 | **`BrandImage` placeholder is a bare letter "N"** on powder. Should be a designed brand tile. |
| 14 | **Image title CTAs overlap the photographs** awkwardly (Ria's tile, Parents' Corner tiles) — the ellipse sits across the subject's face/chin. |
| 15 | **FAQ density and usefulness pass complete.** Eight questions, search, category filters, an expanded sticky sidebar, and a no-results route to the request form replace the unused left and vertical space. **FIXED** |
| 16 | **The empty testimonial band is resolved without fake reviews.** While real, consented quotes are unavailable, the section renders three useful sharing-prompt cards plus a clear consent policy; the real marquee remains gated behind `AWAITING_REAL_TESTIMONIALS`. **FIXED (real quotes blocked on clinic)** |
| 17 | **Booking now uses the full layout.** A branded stroke rail, sticky four-step guide, previous-step navigation, and wider form composition replace the narrow empty-centred page. **FIXED** |
| 18 | **Reveal thresholds and stagger are untuned** across everything written after Services — never verified in a browser until this audit. **FIXED 2026-09-23:** one reveal line for the whole site (`REVEAL_START` in `lib/motion.ts`) and one stagger. |
| 19 | **`Booking.tsx` reformatted and extended** in the same component style as the rest of the site. **FIXED** |

## Open — missing scope

| # | Item |
|---|---|
| 20 | **Footer structure and density pass complete.** The footer is compact, includes useful route links, and no longer leaves a large empty cobalt tail. Contact mock data is development-only and clearly labelled; production still waits for clinic-confirmed address, hours, phone, email, and social links. **FIXED (real facts blocked on clinic)** |
| 21 | **404 is minimal.** Wants the fuller treatment: the elephant, lost, with the tagline arc. |
| 22 | **`TextOnPath` implements only `arc`.** The addendum requires `roundel` and `ring` modes too, each used at least once. `ring` is built and `arc` remains; the roundel is no longer type on a path at all. The client's lollipop board (Aug 2026) supplies the roundel as fixed artwork, so `tools/extract-roundel.py` bakes it to outlines and `<Roundel>` renders it in Hero, Footer and 404. `TextOnPath`'s fake roundel mode is deleted, and neither `arc` nor `ring` currently has a call site. |
| 23 | **2-Minute Brushing Game built.** The interactive timer now appears on the homepage and as the first activity inside the standalone `/games` page. **FIXED** |
| 24 | **Per-route SEO and headings added.** Every navbar destination now has its own pathname, title, description, social metadata, and exactly one `<h1>`; unknown routes are `noindex, nofollow`. **FIXED** |
| 25 | **README is a stub** — currently only the photography-licensing note. Needs setup, the token system, the Avenir Next swap procedure, how to replace placeholder images, and where each brand rule is enforced. |
| 26 | Phase 2 content not started: milestones timeline, service detail, first-visit walkthrough, preparing your child, emergencies, aftercare, practical info, deeper FAQ, legal pages. |
| 27 | Phase 3 through-line (one continuous stroke across the whole site) and route transitions not started. **PARTIAL 2026-09-23:** the journey's scroll-drawn coral thread; a site-wide stroke remains, and route transitions are blocked (item 52). |
| 28 | Phase 4: micro-copy pass, ~~scroll progress indicator~~ (done), ~~favicon set~~ (done), sitemap, Lighthouse run, restraint edit. |

## Mobile pass — 2026-08-01

Every route driven at 390×844 in headless Edge with touch emulation, plus a
reduced-motion pass and a 1440×900 desktop regression check.

| # | Item | Status |
|---|---|---|
| 29 | **Section numbering broke on the mobile home: it counted `00 01 02 03 11`.** `Home.mobile.tsx` deliberately drops seven sections, but every section read its number straight off the `SECTIONS` registry, so `Booking` still announced itself as `11` — the same visible break as item 9, in a new place. Numbers are now counted off the running order a page declares (`<SectionOrder>` / `useSectionMeta` in `content/sectionOrder.tsx`); a section alone on its own route still shows its registry number, which is the brand book's contents number. Mobile home now runs `00–04`; desktop still runs `00–11`. | FIXED |
| 30 | **Mobile nav panel links were 22px tall** — half a thumb. Row spacing moved from the `gap` into each row's own padding, so the targets are 46px without the panel growing much. The active-page rule now tracks the label rather than the padded row. | FIXED |
| 31 | **The open nav panel was powder on the powder hero**, with only its shadow to say where the card stopped and the page began. It now carries a white edge, the same white-on-powder rule its own divider already used. The panel deliberately does not dim the page (see the comment in `Nav.tsx`). | FIXED |
| 32 | **Footer quick links were 20px tall on every route**, and the mobile hero's "Not sure where to start?" was 22px. Both now sit on the `min-h-11` floor the appointment link already used. | FIXED |
| 33 | **`Journey.mobile`'s canary spine ran through the section headline** and out past the last panel, because the SVG was absolutely positioned against the whole section while a 64-unit stroke stretched to full width. It is now scoped to the panel stack it threads — no magic offsets; the panels paint over it on source order. | FIXED |

Checked and clean on mobile after this pass: no horizontal overflow on any
route (`scrollWidth === innerWidth === 390` throughout), exactly one `<h1>` per
route, no heading-level jumps, no rendered `TODO`, no console errors or
warnings, both dev guards silent, and no tap target under 44px. Under
`prefers-reduced-motion: reduce`, all 16 `[data-animate]` elements are visible
and all 91 `[data-draw]` paths render complete.

Not addressed, and still open: the desktop navbar's own links measure 24px tall.
That is a pointer target on a pointer device, so it was left alone in a mobile
pass — but it is worth a decision.

## Mobile redesign patch — 2026-08-16

Applied from `patch-mobile` (client-side design patch, its own README). The
phone home went from three sections to six numbered beats on alternating
surfaces: `Hero` → `JourneyStrip` → `Services` → `Team` → `FindUs` →
`BookBand`, with `<SectionMarker>` as the one wayfinding row and the client
service icons finally used. `ExploreMore.mobile.tsx` and its
`content/exploreMore.ts` were deleted (folded into the footer link grid and
`BookBand`); `Services.tsx` and `Team.tsx` became dispatchers like
`Hero`/`Journey`.

Client review, same day, changed four things from the patch as delivered: the
hero photograph is gone (and with it the full-height hero, which existed only
to give that image somewhere to sit); the `StartHere` beat and its
`content/startHere.ts` are gone, so `SECTIONS` is back to `00–09` and the
hero's "Not sure where to start?" now points at `#journey`; the coral dashes
came off both hero roundels; and the Book band is coral rather than cobalt,
with its copy bedded on a `<TextPanel>`.

| # | Item | Status |
|---|---|---|
| 34 | **Seven text colours in the patch failed AA at their real size.** Cobalt tints on light brand surfaces: the `<SectionMarker>` label at `/75` measured 3.17:1 on powder, the hero's `headlineTail` `/70` 2.90:1, the "Start Here" hints `/80` 3.46:1, the portrait note `/70` on `cobalt-20` 3.17:1, the Find Us hours note `/70` on white 3.82:1. Cobalt on powder is only 4.92:1 before any transparency, so on light surfaces these are now full-strength cobalt (the hours note on white holds at `/80`, 4.83:1). White-on-cobalt tints all had headroom and are unchanged. | FIXED |
| 35 | **Coral section numerals are near-invisible on powder: 1.84:1** (2.79 on paper, 2.39 on canary). They are `aria-hidden` decoration and the desktop `<SectionNumber tone="coral">` has shipped the same way since before this patch, so nothing was changed — but the redesign makes coral numerals the wayfinding on every section, which makes it a live design decision rather than an inherited one. | OPEN |
| 37 | **Client review, round two (phone only).** Two notes beyond the four in item 34's round: the logo story "isn't sure" and should be fitted "as it is in the pdf, same page, a small one"; and the palette should be "a continuous flow" because it "jumps around". Both applied. `<LogoStory>` is the guide's p3 exactly — five glyphs at one size in a single row, four coral either side of the finished cobalt mark, labels alternating above and below on leader lines, no panels and no body copy — and it opens the Services section rather than being a section of its own. The phone home's grounds now step through the palette once and never reverse: powder → paper → canary → coral → cobalt. It previously ran powder, cobalt, paper, canary, powder, coral, visiting cobalt twice and changing direction three times. A third note followed: the hero had gone paper and read as white, so it is back on powder — which is also why the logo story is not in it, since its coral line art measures 1.84:1 on powder against 2.79:1 on paper. | FIXED |
| 38 | **Client review, round three (phone only), on the services list.** Five successive notes, each answering the last: show the treatments "like the desktop version" (rows became desktop's square tiles), "too big, make them small and 6" (three across inside a 26rem cap — `aspect-square` ties tile height to column width, and this component serves up to 767px, so an uncapped two-column grid was producing 350px tiles), "add more" (the six category words became all sixteen named treatments), "fit what you can in the second image space" (two rows of height, which at a legible tile size means a horizontal snap strip, `grid-flow-col` over `grid-rows-2` — a manual swipe, not the auto-scrolling loop rejected earlier), "fit only 10" (`HOME_SLUGS` in `Services.mobile.tsx`: the first nine in clinic order plus Laughing Gas, which is lifted out of order because it is the only treatment with a page of its own; the other six stay one tap away on `/services`), and "empty space, increase each card size" (`minmax(8rem, 1fr)` rather than a fixed column, so five tiles share the width when they fit and swipe when they do not). Nothing in `treatments.ts` was edited — the ten are a display cut of the clinic's own list. | FIXED |
| 36 | **The patch has not been driven in a browser.** Typecheck, production build and the SSR prerender all pass, and the contrast numbers above are measured — but no 390×844 pass, no reduced-motion pass, no `scrollWidth === innerWidth` check. The pre-opening gate (`OPENING_SOON`) serves the holding screen on every route, so the redesign cannot be reached in a browser until that flag is flipped. | OPEN |

## Confirmed working

- New Inside the Clinic section uses three generated, brand-palette concept
  images with an explicit disclosure that they are not real clinic photography.
- Every navbar destination has its own route and route metadata. Standalone
  marketing routes use one consistent coral page-edge rule, and the active
  navbar destination uses a coral underline. The earlier floating corner fields
  were removed because they did not belong to the page layouts.
- The navbar now links to `/games` as **Games for Kids**. That page owns the
  page-level heading and contains the 2-Minute Brushing Game; `/brush-timer`
  remains as a non-indexed compatibility route.
- Full coral fields now have two deliberate uses: Ria's Journey is the
  homepage's coral chapter, and `/games` uses coral across the full page
  content. All copy on those fields is contained by cobalt `TextPanel`s, with
  white loop artwork at brand-book opacity.
- The consecutive canary Brush Timer and Parents' Corner sections on the
  homepage are separated by an 8px cobalt chapter divider.
- The fuller-home pass combines all five requested approaches: tighter
  homepage-only vertical rhythm, restrained loop artwork, an asymmetric
  magazine layout, an accessible interactive selector, and useful route-based
  content. `HomePaths` provides the new `01 Start Here` module without adding
  unverified clinical claims.
- The navbar now hides after a deliberate downward scroll, returns on upward
  scroll, remains available near the top and while the mobile menu is open, and
  exposes a separate coral page-progress rail that never places text on coral.
- `/games` now contains the brushing timer plus three working activities:
  doodle memory pairs, a brushing-order sequence, and a sticker board. The
  mobile handoff between its coral intro and timer was tightened after a real
  390×844 browser check.
- Inside the Clinic now uses a three-choice room selector with one large
  featured concept image; its concept-imagery disclosure remains visible.
  Ria's Journey is now a three-moment storybook with tabs, Previous/Next
  controls, and progress tied to the chosen moment.
- Parents' Corner now has All/Routines/Guides/Products filters and a five-card
  asymmetric mosaic. FAQ has search and category filters. Booking uses a
  two-column desktop composition. Footer spacing is compact and its quick links
  all resolve to current native routes.
- The placeholder Testimonials state now fills its cobalt chapter with honest
  prompt cards and a consent-first note. It still contains no names, review
  claims, or invented quotations.
- Both dev guards silent: no coral-text violation, no sub-64px logo warning.
- No horizontal overflow at 390 or 1440. Heading order has no level jumps.
- Console clean after item 4 (React Router future-flag notices remain, harmless).
- Brand asset system correct throughout — doodles draw, masked variable-width
  artwork animates, `LoopField` and white-at-33% treatments render as intended.
- Images serve WebP and lazy-load correctly.
- Hero, Journey, Services hold up as the house style to match.
