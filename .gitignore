node_modules/
dist/
dist-ssr/
tmp/
*.tsbuildinfo
.DS_Store
*.local
.env
.env.*
!.env.example

supabase/.temp/
supabase/functions/.env

# Release archives produced by `git archive` — build artefacts of a commit,
# so they are never committed back into the repo.
tiny-tusk-website.zip
*.zip

# Python bytecode from the tools/ extraction scripts.
__pycache__/
